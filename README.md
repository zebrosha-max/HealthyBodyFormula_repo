# Healthy Body Formula (Web App)

## 📌 О проекте
Легкое веб-приложение (SPA) для портфолио клинического нутрициолога Юлии. Разработано специально для интеграции как **Telegram Web App** (мини-приложение внутри Telegram), но отлично работает и в обычном мобильном браузере.

## 🛠 Технический стек
*   **HTML5 / CSS3 / Vanilla JS** — использован чистый код без тяжелых фреймворков.
*   **Separation of Concerns:**
    *   `index.html`: Только структура (View).
    *   `css/style.css`: Все стили и анимации.
    *   `js/app.js`: Вся бизнес-логика и взаимодействие.
*   **Single Page Application (SPA)** — все разделы подгружаются динамически без перезагрузки.
*   **Telegram WebApp SDK** — интеграция с клиентом Telegram.
*   **Supabase** — BaaS для базы данных и аутентификации.

## ✅ Реализованный функционал

### 1. Архитектура и Навигация
*   **Система экранов:** Реализовано переключение классов `.active`, что позволяет менять контент (Главная -> Рецепты -> Детальная) плавно и с анимацией `fadeIn`.
*   **History API:** Кнопка "Назад" возвращает на предыдущий логический экран, а не закрывает приложение.

### 2. Раздел "Обо мне"
*   Профиль с фото и био.
*   **Карусель сертификатов:** Горизонтальный скролл дипломов.
*   **Lightbox (Галерея):** При клике на диплом он открывается на весь экран с возможностью зума (кнопки +/-) и прокрутки многостраничных документов.

### 3. Раздел "Рецепты" (База данных JS)
*   **Сетка (Grid):** Отображение карточек в 2 колонки (Instagram-style).
*   **Динамический контент:**
    *   Данные рецептов (ингредиенты, шаги, КБЖУ) хранятся в JavaScript-объекте `recipesDB`.
    *   При клике на карточку не загружается новая страница — скрипт берет данные из базы и "на лету" заполняет шаблон экрана `#screen-recipe-detail`.
*   **Детальная страница:**
    *   Адаптивная шапка с цветом, зависящим от типа блюда.
    *   Список ингредиентов с галочками.
    *   Нумерованный список шагов приготовления.

### 4. Разделы "Гайды" и "Записи"
*   **Гайды:** Список карточек с ценами и кратким описанием. Подготовлено место под функционал скачивания/покупки.
*   **Записи:** Лента медиа-карточек для вебинаров и эфиров.

### 5. Дизайн (UI/UX)
*   **Тема:** Sage Green (Шалфей) + Peach (Персик). Используются CSS-переменные (`:root`) для легкой смены цветовой гаммы.
*   **Анимации:** Плавное появление экранов, эффекты нажатия на кнопки.

## 📂 Структура проекта
```text
HBF_web/
├── index.html          # Точка входа (HTML структура)
├── css/
│   └── style.css       # Основные стили проекта
├── js/
│   └── app.js          # Логика приложения (Auth, Navigation, Recipes)
├── README.md           # Документация проекта
├── docs/               # SDD Документация (Requirements, Architecture)
├── guides/             # Папка с глубокими нутрициологическими протоколами (HTML)
└── Certificates/       # Папка с изображениями дипломов и превью
```

## 🚀 Как запустить локально
Для проверки работы (особенно загрузки локальных файлов) рекомендуется использовать локальный сервер:

```bash
python -m http.server 8080
```
Сайт будет доступен по адресу: `http://localhost:8080`

---

## 🕒 Session History

### 2025-12-21
**Task:** Implement a premium-grade "Anti-Sugar" guide system.

*   **HTML Guide Architecture:**
    *   Transitioned from static PDFs to a dynamic HTML-based "Magazine Style" guide system located in `/guides`.
    *   Implemented `anti-sugar.html` using a scientific "Evidence-Based" approach (biochemistry, 3-phase timeline, troubleshooting).
    *   Designed responsive, accessible UI with expandable Accordions for long-form content.
*   **Seamless Navigation:**
    *   Enhanced `index.html` with a deep-linking mechanism (`URLSearchParams`) to allow "Back" buttons in guides to return users to the specific screen they came from (e.g., `?screen=guides`).
*   **UI/UX Refinements:**
    *   Created a "Glassmorphism" sticky header for guides.
    *   Localized all medical and technical terms for the Russian-speaking audience.
    *   Optimized typography (Lora for body, Nunito for UI) for maximum readability of long nutritional protocols.